---
interface Props {
  title: string;
  description: string;
  link: string;
}

const { title, description, link } = Astro.props;
---

<article>
  <h2>
    <a href={link} data-astro-prefetch class="underline hover:text-stone-600"
      >{title}</a
    >
  </h2>
  <p>{description}</p>
</article>
